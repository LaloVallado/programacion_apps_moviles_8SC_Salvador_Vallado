workflows:
  android-workflow:
    name: Android APK
    max_build_duration: 60
    environment:
      flutter: stable
    scripts:
      - name: Build APK
        script: |
          cd chambita
          flutter pub get
          flutter build apk --release
    artifacts:
      - chambita/build/app/outputs/flutter-apk/*.apk
